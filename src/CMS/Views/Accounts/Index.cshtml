@using Core.Application.Accounts
@model ListAccountsQueryResponse;

<div class="container">
    <header class="mb-3">
        <h1>관리자 계정 목록</h1>
    </header>

    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">아이디</th>
                <th scope="col">상태</th>
                <td scope="col"></td>
            </tr>
            <tr>
                <th colspan="3">
                    <a asp-action="Create" class="btn btn-outline-primary w-100">새 관리자 계정 추가</a>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var account in Model.Data)
            {
                <tr>
                    <td scope="row">
                        @account.Login
                    </td>
                    <td>
                        @if (account.IsEnabled)
                        {
                            <span>
                                <i class='bi bi-check-circle-fill text-success'></i> 활성화됨
                            </span>
                        }
                        else
                        {
                            <span>
                                <i class='bi bi-ban-fill text-danger'></i> 비활성화됨
                            </span>
                        }
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-login="@account.Login">상세보기</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
